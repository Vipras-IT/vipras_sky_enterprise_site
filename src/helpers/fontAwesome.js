import { library } from '@fortawesome/fontawesome-svg-core';
import {
  faChevronRight,
  faSearch,
  faExternalLinkAlt,
} from '@fortawesome/free-solid-svg-icons';

// Add the required icons to the library
library.add(faChevronRight, faSearch, faExternalLinkAlt);
